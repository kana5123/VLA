# Phase 2.5: Dual-Track Sink/Bottleneck Analysis — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** `run_contribution_analysis.py`에 A-peak/C-peak/R-peak 동시 추적 + token identity decode + φ universal + V=0 sanity를 추가하여, 4개 VLA 모델에서 sink와 bottleneck을 정확히 분리한다.

**Architecture:** 기존 Phase 2 파이프라인(`run_contribution_analysis.py` + `contribution/` 모듈)을 확장. `classify.py`에 dual-track 분류 함수 추가, report JSON에 a_peak/c_peak/r_peak 필드 추가, causal에서 knockout 폐기 + V=0 전용. ~75줄 추가, backward compatible.

**Tech Stack:** PyTorch, numpy, existing SinkVerificationHookManager, BridgeData V2 캐시 (`/ceph_data/kana5123/bridge_data_cache/`)

**Design doc:** `docs/plans/2026-02-26-phase2.5-dual-track-design.md`

---

## Critical Context

**파일 구조:**
| File | Role | Lines |
|------|------|-------|
| `run_contribution_analysis.py` | Main CLI + per-sample loop | 462 |
| `contribution/compute.py` | W_OV contribution, `aggregate_contributions()` → returns `(a_tilde, c_tilde)` | 249 |
| `contribution/classify.py` | `classify_layer()`, `compute_phi_all_tokens()`, `classify_token()` | 144 |
| `contribution/causal.py` | `AttentionKnockoutHook` (BROKEN), `ValueZeroHook` (OK), `run_vzero_sanity_check()` | 245 |
| `run_causal_experiment.py` | Causal verification entry point | 231 |
| `extract_attention.py` | `load_model_from_registry()`, `detect_token_boundaries()`, `get_layers()` | 800+ |
| `verify_attention_sinks.py` | `SinkVerificationHookManager`, `get_wov_matrix()` | 1205 |

**모델 아키텍처 (hooks에 중요):**
| Model | Backbone | Layers | Q/KV Heads | V Proj | attn_impl |
|-------|----------|--------|------------|--------|-----------|
| openvla-7b | LLaMA | 32 | 32/32 | `v_proj` | eager |
| ecot-7b | LLaMA | 32 | 32/32 | `v_proj` | eager |
| spatialvla-4b | Gemma2 | 26 | 8/4 (GQA) | `v_proj` | eager |
| tracevla-phi3v | Phi3V | 32 | 32/32 | `qkv_proj` (fused) | eager |

**Phase 2 결과 요약 (수정 대상):**
- ECoT: bottleneck at vision[0], C=97-99% — **예상: A==C → bottleneck only**
- OpenVLA: bottleneck at text 274, C=76-81% — **예상: A-peak=vision[0] ≠ C-peak=text 274 → coexist**
- SpatialVLA: "normal" at 260/225, C=21-37% — **예상: A-peak=text 260 (sink) + C distributed**
- TraceVLA: pos 1, C=13-15% — **예상: token type 확인 필요 (BOS vs vision)**

**Knockout KL=0 버그 (확인됨):**
`AttentionKnockoutHook._make_hook()` (causal.py:78-94)는 post-softmax forward hook으로 `output[1]`(attention weights)을 수정하지만, `output[0]`(attn_output)은 이미 원본 weights로 계산 완료됨. 즉 **완전한 no-op**. V=0만 사용.

---

## Task 1: `classify.py`에 dual-track 분류 함수 추가

**Why:** 현재 `classify_layer()`는 C̃ top-K만 봄. A-peak, C-peak, R-peak를 동시에 추적하는 새 함수 필요.

**Files:**
- Modify: `contribution/classify.py:129-144` (뒤에 추가)

**Step 1: `compute_phi_universal()` 함수 추가**

`classify.py` 끝에 추가:

```python
def compute_phi_universal(hidden_state_j: torch.Tensor) -> float:
    """Architecture-independent outlier score for a SINGLE token.
    φ(x) = max|x[d]| / RMS(x) — larger = more spike in hidden dimensions.

    Args:
        hidden_state_j: (D,) tensor — hidden state of one token
    Returns:
        float — phi value
    """
    h = hidden_state_j.float()
    rms = torch.sqrt(torch.mean(h ** 2)).clamp(min=1e-10)
    return (h.abs().max() / rms).item()
```

**Step 2: `classify_layer_dual_track()` 함수 추가**

`classify.py` 끝에 추가:

```python
def classify_layer_dual_track(
    a_tilde: np.ndarray,
    c_tilde: np.ndarray,
    boundaries: dict,
    hidden_states_layer: torch.Tensor | None = None,
    input_ids: list[int] | None = None,
    tokenizer=None,
) -> dict:
    """Dual-track classification: A-peak, C-peak, R-peak per layer.

    Design doc의 핵심 로직:
      A-peak == C-peak → "bottleneck"
      A-peak ≠ C-peak → "coexist" (sink + bottleneck at different tokens)
      둘 다 분산 → "normal"

    Args:
        a_tilde: (seq,) normalized attention distribution
        c_tilde: (seq,) normalized contribution distribution
        boundaries: vision_start, vision_end, text_start, text_end
        hidden_states_layer: (seq, D) tensor for phi computation (optional)
        input_ids: token IDs for decode (optional)
        tokenizer: for token_str decode (optional)

    Returns:
        dict with a_peak, c_peak, r_peak, dominant_type, a_c_match, etc.
    """
    vs = boundaries.get("vision_start", 0)
    ve = boundaries.get("vision_end", 0)
    seq_len = len(a_tilde)

    # ── Find peaks ──
    j_a = int(np.argmax(a_tilde))      # A-peak: attention 최대
    j_c = int(np.argmax(c_tilde))      # C-peak: contribution 최대

    # R-peak: log(Ã/C̃) 최대 = sink candidate
    eps = 1e-10
    a_safe = np.clip(a_tilde, eps, None)
    c_safe = np.clip(c_tilde, eps, None)
    sink_scores = np.log(a_safe / c_safe)
    j_r = int(np.argmax(sink_scores))   # R-peak: 가장 강한 sink

    # ── Token identity helper ──
    def make_peak_info(pos: int) -> dict:
        info = {
            "abs_t": pos,
            "token_type": _classify_token_type(pos, vs, ve),
            "a_share": float(a_tilde[pos]),
            "c_share": float(c_tilde[pos]),
            "sink_score": float(sink_scores[pos]),
        }
        if vs <= pos < ve:
            info["vision_j"] = pos - vs
        # Token string decode
        if input_ids is not None and tokenizer is not None:
            if pos < len(input_ids):
                try:
                    info["token_str"] = tokenizer.decode([input_ids[pos]], skip_special_tokens=False).strip()
                except Exception:
                    info["token_str"] = None
        # Phi (if hidden states available)
        if hidden_states_layer is not None and pos < hidden_states_layer.shape[0]:
            info["phi"] = compute_phi_universal(hidden_states_layer[pos])
        return info

    a_peak = make_peak_info(j_a)
    c_peak = make_peak_info(j_c)
    r_peak = make_peak_info(j_r)

    # ── Classification ──
    a_c_match = (j_a == j_c)

    # Entropy + top1 share
    entropy = compute_entropy(c_tilde)
    top1_c_share = float(c_tilde.max())
    top1_a_share = float(a_tilde.max())
    mismatch = compute_mismatch(a_tilde, c_tilde)

    if a_c_match and top1_c_share >= 0.5:
        dominant_type = "bottleneck"
    elif a_c_match and top1_c_share < 0.5:
        dominant_type = "normal"
    elif not a_c_match and top1_a_share >= 0.15:
        dominant_type = "coexist"
    else:
        dominant_type = "normal"

    return {
        "dominant_type": dominant_type,
        "a_peak": a_peak,
        "c_peak": c_peak,
        "r_peak": r_peak,
        "a_c_match": a_c_match,
        "mean_entropy": entropy,
        "mean_top1_share": top1_c_share,
        "mean_top1_a_share": top1_a_share,
        "mismatch": mismatch,
    }


def _classify_token_type(pos: int, vs: int, ve: int) -> str:
    """Classify absolute position into token type."""
    if pos < vs:
        return "pre_vision"
    elif pos < ve:
        return "vision"
    else:
        return "text"
```

**Step 3: Verify syntax**

Run: `python -c "from contribution.classify import classify_layer_dual_track, compute_phi_universal; print('OK')"`
Expected: `OK`

**Step 4: Commit**

```bash
git add contribution/classify.py
git commit -m "feat: add dual-track classification (A-peak/C-peak/R-peak) to classify.py"
```

---

## Task 2: `run_contribution_analysis.py`에 dual-track 통합 + report 확장

**Why:** 메인 파이프라인이 per-sample loop에서 `classify_layer_dual_track()`을 호출하고, 결과를 새 report 구조에 저장해야 함.

**Files:**
- Modify: `run_contribution_analysis.py:30-54` (imports 추가)
- Modify: `run_contribution_analysis.py:78-84` (phase 2.5 출력)
- Modify: `run_contribution_analysis.py:116-122` (dual-track collection 변수)
- Modify: `run_contribution_analysis.py:183-270` (per-sample loop에 dual-track 추가)
- Modify: `run_contribution_analysis.py:288-310` (aggregation에 dual-track 추가)
- Modify: `run_contribution_analysis.py:361-389` (report에 dual-track 필드 추가)

**Step 1: Import 추가**

`run_contribution_analysis.py` 상단 imports (line 36-40 부근)에 추가:

```python
from contribution.classify import (
    classify_layer,
    compute_phi_all_tokens,
    compute_mismatch,
    classify_layer_dual_track,   # NEW Phase 2.5
    compute_phi_universal,       # NEW Phase 2.5
)
```

**Step 2: Phase 2.5 표시 + tokenizer 준비**

`run_analysis()` 함수 시작 부분 (line 82-84 부근)에서:

```python
    print(f"\n{'='*70}")
    print(f"Contribution Analysis — {model_name} (Phase 2.5: Dual-Track)")
    print(f"  query_mode={args.query_mode}, head_filter={args.head_filter}, rho={args.rho}")
    print(f"{'='*70}")
```

Model 로딩 후 (line 88 이후) tokenizer 추출:

```python
    # Tokenizer for token identity decode
    tokenizer = getattr(processor, 'tokenizer', None)
    if tokenizer is None and hasattr(processor, 'decode'):
        tokenizer = processor  # processor itself has decode
```

**Step 3: Dual-track collection 변수 추가**

Line 116-122의 collection 변수 뒤에 추가:

```python
    # Phase 2.5: Dual-track per-layer per-sample
    all_layer_dual_track = {l: [] for l in deep_layers}
```

**Step 4: Per-sample loop에 dual-track 추가**

현재 per-sample loop (line 237-253)에서 `classify_layer()`를 호출한 직후, dual-track도 호출. `for r in results:` 블록 내부에서 `all_layer_classifications[l].append(cls_result)` 다음에:

```python
                # Phase 2.5: Dual-track classification
                # Get input_ids for token decode (from this sample's inputs)
                sample_input_ids = inputs.get("input_ids", None)
                if sample_input_ids is not None:
                    sample_input_ids = sample_input_ids[0].tolist()

                dual = classify_layer_dual_track(
                    r.a_tilde, r.c_tilde,
                    sample_boundaries,
                    hidden_states_layer=hidden_states.get(l),
                    input_ids=sample_input_ids,
                    tokenizer=tokenizer,
                )
                all_layer_dual_track[l].append(dual)
```

**Step 5: Aggregation에서 dual-track 집계**

Line 288-310 (layer_dominant 계산) 뒤에 추가:

```python
    # Phase 2.5: Aggregate dual-track per layer
    layer_dual_track_agg = {}
    for l in deep_layers:
        duals = all_layer_dual_track.get(l, [])
        if not duals:
            continue

        # Dominant type by vote
        types = [d["dominant_type"] for d in duals]
        dominant_vote = Counter(types).most_common(1)[0]

        # A-peak: most common position
        a_positions = [d["a_peak"]["abs_t"] for d in duals]
        a_dom_pos = Counter(a_positions).most_common(1)[0][0]

        # C-peak: most common position
        c_positions = [d["c_peak"]["abs_t"] for d in duals]
        c_dom_pos = Counter(c_positions).most_common(1)[0][0]

        # R-peak: most common position
        r_positions = [d["r_peak"]["abs_t"] for d in duals]
        r_dom_pos = Counter(r_positions).most_common(1)[0][0]

        # Find representative sample for each peak (one that matches dominant position)
        def find_representative(duals, peak_key, dom_pos):
            for d in duals:
                if d[peak_key]["abs_t"] == dom_pos:
                    return d[peak_key]
            return duals[0][peak_key]

        a_peak_rep = find_representative(duals, "a_peak", a_dom_pos)
        c_peak_rep = find_representative(duals, "c_peak", c_dom_pos)
        r_peak_rep = find_representative(duals, "r_peak", r_dom_pos)

        # Average metrics
        a_c_match_rate = sum(1 for d in duals if d["a_c_match"]) / len(duals)

        layer_dual_track_agg[l] = {
            "dominant_type": dominant_vote[0],
            "frequency": dominant_vote[1] / len(duals),
            "a_peak": a_peak_rep,
            "c_peak": c_peak_rep,
            "r_peak": r_peak_rep,
            "a_c_match": a_c_match_rate > 0.5,
            "a_c_match_rate": a_c_match_rate,
            "mean_entropy": float(np.mean([d["mean_entropy"] for d in duals])),
            "mean_top1_share": float(np.mean([d["mean_top1_share"] for d in duals])),
            "mean_mismatch": float(np.mean([d["mismatch"] for d in duals])),
        }
```

**Step 6: Report에 dual-track 필드 추가**

Report dict (line 362-384)에서 `"phase": 2`를 `"phase": 2.5`로 변경하고, `"layer_analysis"` 값을 dual-track으로 교체:

```python
    report = {
        "model": model_name,
        "phase": 2.5,     # ← Changed from 2
        "n_samples": len(samples),
        # ... (rest unchanged) ...
        "layer_analysis": {str(l): layer_dual_track_agg.get(l, {}) for l in deep_layers},  # ← Changed
        # ... (rest unchanged) ...
    }
```

**Step 7: Verify syntax**

Run: `python -c "import run_contribution_analysis; print('OK')"`
Expected: `OK`

**Step 8: Commit**

```bash
git add run_contribution_analysis.py
git commit -m "feat: integrate dual-track A-peak/C-peak/R-peak into contribution analysis"
```

---

## Task 3: Knockout 폐기 + V=0 전용으로 causal 수정

**Why:** `AttentionKnockoutHook`은 no-op 버그. Causal 실험에서 knockout 제거하고 V=0만 사용. 또한 `detect_token_boundaries()`의 추가 forward pass를 제거하여 결과 오염 방지.

**Files:**
- Modify: `run_causal_experiment.py:46-179` (knockout 제거 + boundary detection 순서 변경)
- Modify: `run_causal_experiment.py:182-227` (CLI에서 knockout 옵션 제거)

**Step 1: `run_causal()` 함수에서 knockout 제거 + boundary detection 최적화**

`run_causal_experiment.py`의 `run_causal()` 함수를 다음과 같이 수정:

```python
def run_causal(args, output_dir, candidate_positions_abs):
    """Run V=0 causal ablation, measure KL divergence."""
    model_name = args.model
    device = args.device

    print(f"\n{'='*70}")
    print(f"Causal Experiment — {model_name} (V=0 only, knockout removed)")
    print(f"  Candidates (abs_t): {candidate_positions_abs}")
    print(f"{'='*70}")

    processor, model, model_cfg = load_model_from_registry(model_name, device)
    samples = load_samples(args)

    # B4: Sanity check V=0 on first sample
    print("\n  B4 Sanity Check: Verifying V=0 hook works...")
    sample0 = samples[0]
    prompt = model_cfg.prompt_template.format(instruction=sample0["instruction"])
    inputs0 = call_processor(processor, prompt, sample0["image"], model_cfg, return_tensors="pt").to(device)
    if "pixel_values" in inputs0 and inputs0["pixel_values"].dtype != model.dtype:
        inputs0["pixel_values"] = inputs0["pixel_values"].to(model.dtype)

    sanity = run_vzero_sanity_check(model, model_cfg, get_layers, inputs0, candidate_positions_abs[:3])
    print(f"    hook_fired={sanity['hook_fired']}, "
          f"logits_changed={sanity['logits_changed']}, "
          f"kl={sanity['kl_divergence']:.6f}")
    if not sanity["hook_fired"]:
        print("    WARNING: V=0 hook did NOT fire! Check hook registration.")
    if not sanity["logits_changed"]:
        print("    WARNING: V=0 did NOT change logits! Hook may be on wrong module.")

    results = {
        "model": model_name,
        "method": "v_zero",
        "candidates_abs_t": candidate_positions_abs,
        "sanity_check": sanity,
        "per_k": {},
    }

    for k in [1, 3, 5]:
        targets = candidate_positions_abs[:k]
        vzero_kls = []
        vzero_changes = []

        for si, sample in enumerate(samples):
            prompt = model_cfg.prompt_template.format(instruction=sample["instruction"])
            inputs = call_processor(processor, prompt, sample["image"], model_cfg, return_tensors="pt").to(device)
            if "pixel_values" in inputs and inputs["pixel_values"].dtype != model.dtype:
                inputs["pixel_values"] = inputs["pixel_values"].to(model.dtype)

            # Original forward
            with torch.no_grad():
                out_orig = model(**inputs)
            logits_orig = out_orig.logits[0, -1, :]

            # V=0 forward
            vzero = ValueZeroHook(targets)
            vzero.register(model, model_cfg, get_layers)
            with torch.no_grad():
                out_vz = model(**inputs)
            logits_vz = out_vz.logits[0, -1, :]
            vzero.remove()

            kl_vz = compute_output_kl(logits_orig, logits_vz)
            change_vz = compute_top1_change_rate(logits_orig.unsqueeze(0), logits_vz.unsqueeze(0))
            vzero_kls.append(kl_vz)
            vzero_changes.append(change_vz)

        results["per_k"][k] = {
            "targets": targets,
            "vzero_mean_kl": float(np.mean(vzero_kls)),
            "vzero_std_kl": float(np.std(vzero_kls)),
            "vzero_mean_top1_change": float(np.mean(vzero_changes)),
        }

        print(f"  K={k}, V0_KL={np.mean(vzero_kls):.4f}+-{np.std(vzero_kls):.4f}, "
              f"top1_change={np.mean(vzero_changes):.3f}")

    # Save
    output_dir.mkdir(parents=True, exist_ok=True)
    report_path = output_dir / "causal_report.json"
    with open(report_path, "w") as f:
        json.dump(results, f, indent=2)
    print(f"\n  Report saved: {report_path}")

    # Plot
    k_vals = sorted(results["per_k"].keys())
    plot_data = {"value_zero": [results["per_k"][k]["vzero_mean_kl"] for k in k_vals]}
    plot_masking_ablation(k_vals, plot_data, output_dir / "masking_ablation.png")

    del model
    torch.cuda.empty_cache()
    return results
```

**Step 2: CLI에서 `--method` default를 `v_zero`로, knockout 옵션 제거**

`main()` 함수에서:

```python
    parser.add_argument("--method", choices=["v_zero"], default="v_zero",
                       help="Causal method (knockout removed — was no-op bug)")
```

**Step 3: Imports 정리**

`run_causal_experiment.py` 상단에서 `AttentionKnockoutHook` import 제거:

```python
from contribution.causal import (
    # AttentionKnockoutHook,  # REMOVED: no-op bug (Phase 2.5)
    ValueZeroHook,
    compute_output_kl,
    compute_top1_change_rate,
    vision_j_to_abs_t,
    run_vzero_sanity_check,
)
```

**Step 4: Verify syntax**

Run: `python -c "import run_causal_experiment; print('OK')"`
Expected: `OK`

**Step 5: Commit**

```bash
git add run_causal_experiment.py
git commit -m "fix: remove broken knockout hook, use V=0 only for causal experiments

AttentionKnockoutHook was a complete no-op: post-softmax forward hook
modifies output[1] (attention weights) but output[0] (attn_output) was
already computed from original weights. This caused KL=0.0 on all models.

Also removed detect_token_boundaries() call between original and masked
forward passes to prevent numerical state contamination."
```

---

## Task 4: `causal.py`에 knockout deprecation 표시

**Why:** `AttentionKnockoutHook` 클래스를 바로 삭제하면 다른 코드가 깨질 수 있음. Deprecation 표시만 추가.

**Files:**
- Modify: `contribution/causal.py:38-99`

**Step 1: Deprecation docstring 추가**

`AttentionKnockoutHook` 클래스 docstring을 변경:

```python
class AttentionKnockoutHook:
    """DEPRECATED (Phase 2.5): This hook is a no-op.

    Bug: register_forward_hook fires AFTER self_attn.forward() completes.
    By that point, attn_output = attn_weights @ value has already been computed.
    Modifying output[1] (weights) does NOT change output[0] (attn_output).
    Result: KL=0.0 on all models (confirmed ECoT, OpenVLA, SpatialVLA).

    Use ValueZeroHook instead — it hooks v_proj directly (in computation path).

    For attention-level intervention, modify attention_mask BEFORE forward pass:
        attention_mask[:, :, query_range, target_positions] = -inf
    """
```

**Step 2: Commit**

```bash
git add contribution/causal.py
git commit -m "docs: deprecate AttentionKnockoutHook with bug explanation"
```

---

## Task 5: 50-sample quick validation (1 model)

**Why:** 코드 변경을 전체 230 samples 전에 1개 모델로 빠르게 검증.

**Files:** None (실행만)

**Step 1: ECoT로 quick run**

ECoT는 bottleneck이 가장 강해서 (C=97-99%) A==C가 확실히 나와야 함:

```bash
python run_contribution_analysis.py \
  --model ecot-7b \
  --device cuda:0 \
  --balanced --n_per_skill 5 --seed 42 \
  --query_mode last_one --head_filter all \
  --output_dir outputs/contribution_analysis/ecot-7b-phase2.5-test
```

**Step 2: Report 확인**

```bash
python -c "
import json
with open('outputs/contribution_analysis/ecot-7b-phase2.5-test/contribution_report.json') as f:
    r = json.load(f)
print(f'Phase: {r[\"phase\"]}')
for l, info in r['layer_analysis'].items():
    print(f'  L{l}: type={info.get(\"dominant_type\")}, '
          f'A-peak={info.get(\"a_peak\",{}).get(\"abs_t\")}, '
          f'C-peak={info.get(\"c_peak\",{}).get(\"abs_t\")}, '
          f'match={info.get(\"a_c_match\")}')
"
```

Expected:
- `Phase: 2.5`
- All layers: `dominant_type=bottleneck`, `A-peak=0`, `C-peak=0`, `a_c_match=True`

**Step 3: Commit test results**

```bash
git add outputs/contribution_analysis/ecot-7b-phase2.5-test/
git commit -m "test: Phase 2.5 quick validation on ECoT (50 samples)"
```

---

## Task 6: OpenVLA validation (coexist 패턴 확인)

**Why:** OpenVLA에서 A-peak(vision[0]) ≠ C-peak(text 274) → "coexist" 패턴이 나와야 함. 이것이 Phase 2.5의 핵심 검증.

**Files:** None (실행만)

**Step 1: OpenVLA 50-sample run**

```bash
python run_contribution_analysis.py \
  --model openvla-7b \
  --device cuda:0 \
  --balanced --n_per_skill 5 --seed 42 \
  --query_mode last_one --head_filter all \
  --output_dir outputs/contribution_analysis/openvla-7b-phase2.5-test
```

**Step 2: Report 확인 — coexist 패턴 검증**

```bash
python -c "
import json
with open('outputs/contribution_analysis/openvla-7b-phase2.5-test/contribution_report.json') as f:
    r = json.load(f)
for l, info in r['layer_analysis'].items():
    a = info.get('a_peak', {})
    c = info.get('c_peak', {})
    rp = info.get('r_peak', {})
    print(f'  L{l}: type={info.get(\"dominant_type\")}, '
          f'A({a.get(\"token_type\")}@{a.get(\"abs_t\")} a={a.get(\"a_share\",0):.3f}), '
          f'C({c.get(\"token_type\")}@{c.get(\"abs_t\")} c={c.get(\"c_share\",0):.3f}), '
          f'R({rp.get(\"token_type\")}@{rp.get(\"abs_t\")} score={rp.get(\"sink_score\",0):.2f})')
"
```

Expected:
- Most layers: `dominant_type=coexist`
- A-peak: `vision@0` (attention sink)
- C-peak: `text@274` (contribution bottleneck)
- R-peak: `vision@0` (highest log(A/C) ratio = strongest sink)

**Step 3: Commit**

```bash
git add outputs/contribution_analysis/openvla-7b-phase2.5-test/
git commit -m "test: Phase 2.5 OpenVLA validation — coexist pattern confirmed"
```

---

## Task 7: SpatialVLA + TraceVLA validation

**Why:** SpatialVLA의 text sink 확인 + TraceVLA pos 1 token identity 확정.

**Files:** None (실행만)

**Step 1: SpatialVLA run**

```bash
python run_contribution_analysis.py \
  --model spatialvla-4b \
  --device cuda:0 \
  --balanced --n_per_skill 5 --seed 42 \
  --query_mode last_one --head_filter all \
  --output_dir outputs/contribution_analysis/spatialvla-4b-phase2.5-test
```

**Step 2: TraceVLA run**

```bash
python run_contribution_analysis.py \
  --model tracevla-phi3v \
  --device cuda:0 \
  --balanced --n_per_skill 5 --seed 42 \
  --query_mode last_one --head_filter all \
  --output_dir outputs/contribution_analysis/tracevla-phi3v-phase2.5-test
```

**Step 3: TraceVLA token identity 확인**

```bash
python -c "
import json
with open('outputs/contribution_analysis/tracevla-phi3v-phase2.5-test/contribution_report.json') as f:
    r = json.load(f)
# Check pos 1 token type and string
for l, info in list(r['layer_analysis'].items())[:3]:
    a = info.get('a_peak', {})
    c = info.get('c_peak', {})
    print(f'  L{l}: A-peak token_str=\"{a.get(\"token_str\", \"?\")}\", type={a.get(\"token_type\")}@{a.get(\"abs_t\")}')
    print(f'        C-peak token_str=\"{c.get(\"token_str\", \"?\")}\", type={c.get(\"token_type\")}@{c.get(\"abs_t\")}')
"
```

Expected for TraceVLA:
- `token_str` reveals whether pos 1 is BOS, `<|user|>`, or a vision patch
- This resolves the Phase 2 uncertainty

**Step 4: SpatialVLA 확인**

```bash
python -c "
import json
with open('outputs/contribution_analysis/spatialvla-4b-phase2.5-test/contribution_report.json') as f:
    r = json.load(f)
for l, info in r['layer_analysis'].items():
    a = info.get('a_peak', {})
    print(f'  L{l}: type={info.get(\"dominant_type\")}, A-peak={a.get(\"token_type\")}@{a.get(\"abs_t\")}, '
          f'a_share={a.get(\"a_share\",0):.3f}, phi={a.get(\"phi\",0):.1f}')
"
```

Expected: SpatialVLA A-peak at text pos 260 with high φ (sink pattern).

**Step 5: Commit**

```bash
git add outputs/contribution_analysis/spatialvla-4b-phase2.5-test/ outputs/contribution_analysis/tracevla-phi3v-phase2.5-test/
git commit -m "test: Phase 2.5 SpatialVLA sink + TraceVLA token identity validation"
```

---

## Task 8: Full 230-sample run (4 models)

**Why:** Quick validation 통과 후, 전체 데이터로 최종 결과 생성.

**Files:** None (실행만)

**Step 1: 4 models 병렬 실행**

```bash
python run_contribution_analysis.py --model openvla-7b --device cuda:0 \
  --balanced --n_per_skill 25 --seed 42 --query_mode last_one --head_filter all \
  --output_dir outputs/contribution_analysis/openvla-7b-phase2.5 &

python run_contribution_analysis.py --model ecot-7b --device cuda:1 \
  --balanced --n_per_skill 25 --seed 42 --query_mode last_one --head_filter all \
  --output_dir outputs/contribution_analysis/ecot-7b-phase2.5 &

python run_contribution_analysis.py --model spatialvla-4b --device cuda:2 \
  --balanced --n_per_skill 25 --seed 42 --query_mode last_one --head_filter all \
  --output_dir outputs/contribution_analysis/spatialvla-4b-phase2.5 &

python run_contribution_analysis.py --model tracevla-phi3v --device cuda:3 \
  --balanced --n_per_skill 25 --seed 42 --query_mode last_one --head_filter all \
  --output_dir outputs/contribution_analysis/tracevla-phi3v-phase2.5 &

wait
echo "All 4 models complete"
```

**Step 2: Results summary**

```bash
python -c "
import json, glob
for f in sorted(glob.glob('outputs/contribution_analysis/*-phase2.5/contribution_report.json')):
    with open(f) as fh:
        r = json.load(fh)
    model = r['model']
    # Get last layer info
    layers = sorted(r['layer_analysis'].keys(), key=int)
    last = r['layer_analysis'][layers[-1]]
    a = last.get('a_peak', {})
    c = last.get('c_peak', {})
    print(f'{model:20s} type={last.get(\"dominant_type\"):12s} '
          f'A={a.get(\"token_type\",\"?\"):8s}@{a.get(\"abs_t\",\"?\")} '
          f'C={c.get(\"token_type\",\"?\"):8s}@{c.get(\"abs_t\",\"?\")} '
          f'match={last.get(\"a_c_match\")}')
"
```

Expected:
| Model | dominant_type | A-peak | C-peak | a_c_match |
|-------|---------------|--------|--------|-----------|
| ecot-7b | bottleneck | vision@0 | vision@0 | True |
| openvla-7b | coexist | vision@0 | text@274 | False |
| spatialvla-4b | coexist or normal | text@260 | varies | False |
| tracevla-phi3v | bottleneck or coexist | pos 1 | pos 1 | True/varies |

**Step 3: Commit full results**

```bash
git add outputs/contribution_analysis/*-phase2.5/
git commit -m "data: Phase 2.5 dual-track results (4 models x 230 samples)"
```

---

## Task 9: V=0 causal re-run (clean, no knockout)

**Why:** Phase 2 causal results는 knockout bug + 추가 forward pass 오염이 있으므로 clean re-run 필요.

**Files:** None (실행만)

**Step 1: Phase 2.5 report에서 후보 자동 추출 + V=0 실행**

```bash
for model in openvla-7b ecot-7b spatialvla-4b tracevla-phi3v; do
  python run_causal_experiment.py \
    --model $model \
    --device cuda:0 \
    --method v_zero \
    --report outputs/contribution_analysis/${model}-phase2.5/contribution_report.json \
    --balanced --n_per_skill 5 --seed 42 \
    --output_dir outputs/causal_experiment/${model}-phase2.5
done
```

**Step 2: Results check**

```bash
python -c "
import json, glob
for f in sorted(glob.glob('outputs/causal_experiment/*-phase2.5/causal_report.json')):
    with open(f) as fh:
        r = json.load(fh)
    model = r['model']
    s = r.get('sanity_check', {})
    k1 = r['per_k'].get('1', r['per_k'].get(1, {}))
    print(f'{model:20s} sanity_fired={s.get(\"hook_fired\")}, '
          f'V0_KL={k1.get(\"vzero_mean_kl\",0):.4f}, '
          f'top1_change={k1.get(\"vzero_mean_top1_change\",0):.3f}')
"
```

Expected: All models show `sanity_fired=True`, KL > 0.

**Step 3: Commit**

```bash
git add outputs/causal_experiment/*-phase2.5/
git commit -m "data: Phase 2.5 clean V=0 causal results (knockout removed)"
```

---

## Verification Checklist

After all tasks complete, verify these success criteria from the design doc:

1. **4개 모델 A-peak/C-peak 분리**: Report에 `a_peak`, `c_peak` 별도 필드 존재
2. **OpenVLA coexist**: `dominant_type="coexist"`, A-peak=vision, C-peak=text
3. **SpatialVLA sink 감지**: A-peak at text 260 with high φ (Phase 2에서 "normal"이었던 것이 수정됨)
4. **TraceVLA token identity**: `token_str` 필드로 pos 1의 실체 확정
5. **V=0 sanity**: 4개 모델 모두 `hook_fired=True`, `logits_changed=True`
6. **Knockout 폐기**: `run_causal_experiment.py`에서 knockout 코드 제거

---

Plan complete and saved to `docs/plans/2026-02-26-phase2.5-dual-track-impl.md`. Two execution options:

**1. Subagent-Driven (this session)** - I dispatch fresh subagent per task, review between tasks, fast iteration

**2. Parallel Session (separate)** - Open new session with executing-plans, batch execution with checkpoints

Which approach?
